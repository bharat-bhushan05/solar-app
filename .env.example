# Example `.env` file. Copy this to `.env` and fill real values. DO NOT commit secrets.
# This project reads these environment variables at runtime.

# MongoDB connection (replace with your connection string)
MONGO_URI=
# Optional: username/password if your MongoDB requires them
MONGO_USERNAME=
MONGO_PASSWORD=

# Port the app listens on (default 3000)
PORT=3000

# Mongoose strictQuery behavior (controls Mongoose query casting/strictness)
# Set to "true" to enable strictQuery, "false" to disable. If unset the app
# defaults to `false` to prepare for Mongoose v7 behavior.
# Examples:
# MONGOOSE_STRICT_QUERY=true
# MONGOOSE_STRICT_QUERY=false
MONGOOSE_STRICT_QUERY=false

# Notes:
# - Keep this file out of source control; add `.env` to `.gitignore` if needed.
# - If your password or other values contain special characters, URL-encode them
#   when embedding in `MONGO_URI` (use `encodeURIComponent` in scripts).